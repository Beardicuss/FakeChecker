name = "fake-checker-ai-worker"
main = "src/index.js"
compatibility_date = "2024-04-05"

# The KV store where we will cache the daily AI output 
[[kv_namespaces]]
binding = "GAME_STATE"
id = "pending-id-setup" # We will need to create this later when deploying

# Cron trigger: Runs every 6 hours
# Commented out so it doesn't run while we code
# [triggers]
# crons = ["0 */6 * * *"]
